apply plugin: 'com.android.application'

android {
    namespace "com.rjp.thebteam"

    compileSdk 35

    defaultConfig {
        applicationId "com.rjp.thebteam"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.7.0'
}
